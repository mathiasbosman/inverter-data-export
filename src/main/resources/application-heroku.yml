# This property file is used by the Heroku platform. We use environment variables to inject the values
alphaess:
  credentials:
    username: ${ALPHAESS_USERNAME}
    password: ${ALPHAESS_PASSWORD}
energyid:
  mock: ${ENERGYID_MOCK}
  secret-uri: ${ENERGYID_SECRET_URL}
  meters:
    - inverterId: ${ALPHAESS_SN}
      remoteId: "solarAlphaEss"
      remoteName: "solarAlphaEss"
      metric: "solarPhotovoltaicProduction"
      unit: "kWh"
      readingType: "premarkedInterval"